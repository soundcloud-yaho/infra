grafana:
  admin:
    existingSecret: "grafana-admin-secret"
    userKey: admin-user
    passwordKey: admin-password
  service:
    type: ClusterIP
  nodeSelector:
    role: system
  sidecar:
    dashboards:
      enabled: true
      label: grafana_dashboard
      searchNamespace: monitoring

prometheus:
  prometheusSpec:
    replicas: 1
    topologySpreadConstraints: []
    serviceMonitorSelectorNilUsesHelmValues: false
    retention: 45d
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: gp3
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 30Gi
    nodeSelector:
      role: system
      topology.kubernetes.io/zone: ap-northeast-2a  # 2a 고정
  service:
    type: ClusterIP

alertmanager:
  alertmanagerSpec:
    nodeSelector:
      role: system

kube-state-metrics:
  nodeSelector:
    role: system
  metricLabelsAllowlist:
    - nodes=[karpenter.sh/nodepool,karpenter.sh/capacity-type,eks.amazonaws.com/nodegroup]

prometheusOperator:
  nodeSelector:
    role: system

prometheus-node-exporter:
  tolerations:
    - operator: Exists
