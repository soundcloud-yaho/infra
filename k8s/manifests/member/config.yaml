# [Member] DB 및 JWT 설정 — 비밀값은 Secret에서 별도 주입
apiVersion: v1
kind: ConfigMap
metadata:
  name: member-config
  namespace: app
data:
  APP_NAME: "Member Service API"
  APP_PORT: "8080"
  DB_HOST: "soundcloud-prod-aurora.cluster-cf42qiw2429n.ap-northeast-2.rds.amazonaws.com"
  DB_PORT: "5432"
  DB_NAME: "member"
  DB_USER: "postgres"
  JWT_ALGORITHM: "HS256"
  JWT_EXPIRE_MINUTES: "60"