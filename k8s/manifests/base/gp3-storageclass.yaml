apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: neuralprophet-model
  namespace: ai
  labels:
    app: neuralprophet
    component: train
spec:
  storageClassName: gp3
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi