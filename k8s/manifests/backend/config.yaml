# [Backend] DB 접속 설정 — 비밀 아닌 값만. DB_PASSWORD는 aurora-db-secret(Secret)에서 별도 주입
apiVersion: v1
kind: ConfigMap
metadata:
  name: db-config
  namespace: app
data:
  DB_WRITER_HOST: "soundcloud-prod-aurora.cluster-cf42qiw2429n.ap-northeast-2.rds.amazonaws.com"
  DB_READER_HOST: "soundcloud-prod-aurora.cluster-ro-cf42qiw2429n.ap-northeast-2.rds.amazonaws.com"
  DB_USER: "app_admin"
  DB_NAME: "worldcup"
  DB_PORT: "5432"