# [Comment] DB 및 JWT 설정 — 비밀 아닌 값만. 비밀번호와 JWT 키는 Secret에서 별도 주입
apiVersion: v1
kind: ConfigMap
metadata:
  name: comment-config
  namespace: app
data:
  APP_NAME: "Comment Service"
  APP_PORT: "8080"
  DB_HOST: "soundcloud-prod-aurora.cluster-cf42qiw2429n.ap-northeast-2.rds.amazonaws.com"
  DB_PORT: "5432"
  DB_NAME: "comment"
  DB_USER: "postgres"
  JWT_ALGORITHM: "HS256"